'use client';

// The full-screen region-selector popup on first visit has been intentionally removed.
// Region selection is now handled exclusively through the header dropdown.

export default function RegionSelector() {
  return null;
}
